{
  "name": "hoopfolio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Free-to-play NBA player stock market prototype with local cache-first data.",
  "scripts": {
    "start": "node scripts/dev-server.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
